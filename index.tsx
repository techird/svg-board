import * as React from "react";
import * as ReactDOM from "react-dom";

import { CounterAppContainer } from "./modules/counter";

ReactDOM.render(<CounterAppContainer />, document.getElementById("container"));
